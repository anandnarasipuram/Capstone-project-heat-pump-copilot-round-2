# Heat Pump Copilot — Round 2 MVP
# Tested against Python 3.11/3.12. Install with: pip install -r requirements.txt

streamlit>=1.38
pandas>=2.2
altair>=5.0          # Judge Reports page charts (already a Streamlit dependency, pinned here since app.py imports it directly)
python-dotenv>=1.0.1

# Core AI capability: OpenAI classification + embeddings, via LangChain,
# retrieved from a Pinecone serverless vector store.
openai>=1.54.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-pinecone>=0.2.0
pinecone>=5.0.0

# Monitoring — traces every AI call (retrieval + all 3 LLM call sites) to
# LangSmith. Import-safe with zero keys (tracing is simply off); see
# core/tracing.py.
langsmith>=0.1.140

# Dev / testing (offline unit tests only — no API keys needed)
pytest>=8.0
